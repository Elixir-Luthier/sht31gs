# Sht31gs

An example OTP GenServer application demonstrating how one would use ElixirALE to write an IoT applicatation.

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `sht31gs` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [{:sht31gs, "~> 0.1.0"}]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at [https://hexdocs.pm/sht31gs](https://hexdocs.pm/sht31gs).

